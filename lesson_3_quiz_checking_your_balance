

var checkBalance = prompt("Would you like to use the ATM to check your balance? \nEnter True for yes or False for no ");
checkBalance = checkBalance.toLowerCase(); //makes checkBalance lowercase

if (checkBalance === "true") {
	var balance = prompt("What is your account balance: ");

	var isActive = prompt("Is  your account active?: ");
	isActive = active.toLowerCase(); //make active lowercase
}

if (checkBalance === "true") {
	if (isActive === "yes" && balance > 0) {
		console.log("Your balance is $" + balance.toFixed(2) + ".");
	} else if (isActive !== "yes") {
		console.log("Your account is no longer active");
	} else if (balance === 0) {
		console.log("Your account is empty.");
	} else {
		console.log("Your balance is negative. Please contact bank.");
	}
	
} else {
	console.log("Thank you. Have a nice day!");
}